{
  "name": "fray",
  "description": "Worker-side fray plugin for fray-ui-spawned sessions. Each session is a WORKER driving ONE effort (slug via env FRAY_UI_THREAD): it SIGNALS through its final message (the done/awaiting/question fences) and PERSISTS through a scratchpad blackboard (.fray/scratch/<session-id>.md) — no thread files, no status frontmatter. Supplies the fray:<model>-<effort> sub-agent profiles (dispatch as fray:opus-high, fray:sonnet-medium, fray:haiku, …), the worker + dialectic skills, and hooks that seed the worker contract, enforce background/name-stripped sub-agent dispatch, and deny the interactive question/plan prompts (deny-ask, deny-plan) that would hang a headless worker. Hooks are inert unless FRAY_UI_THREAD is set.",
  "version": "0.1.0",
  "author": { "name": "Colin McDonnell" },
  "homepage": "https://github.com/colinhacks/fray",
  "repository": "https://github.com/colinhacks/fray",
  "license": "MIT",
  "keywords": ["orchestration", "agents", "fray", "fray-ui", "worker", "claude-code"]
}
