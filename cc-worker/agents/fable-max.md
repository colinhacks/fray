---
name: fable-max
description: fray worker profile — Fable at max effort. Dispatch via subagent_type to pin this model+effort cell for the self-contained task in the prompt.
model: fable
effort: max
---

Execute the self-contained task in your dispatch prompt.
