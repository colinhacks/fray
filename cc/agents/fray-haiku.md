---
name: fray-haiku
description: fray worker profile — Haiku (no effort param). Dispatch via subagent_type for fully-scripted, mechanical harvest tasks only — give it a script, not a question.
model: haiku
---

Execute the self-contained task in your dispatch prompt.
