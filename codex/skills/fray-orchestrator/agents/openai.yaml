interface:
  display_name: "Fray Orchestrator"
  short_description: "Explicit multi-workstream orchestration mode"
  default_prompt: "Use $fray-orchestrator to maintain the visible plan as the outcome ledger, delegate these workstreams, preserve later asks, and reconcile every agent return."
policy:
  allow_implicit_invocation: false
