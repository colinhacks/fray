// Every inline prompt-box dropdown (model, effort, permission, and sandbox) uses this exact
// type treatment. It intentionally contains no responsive or compact size variant: compact
// controls may reduce padding/icon size, never the selected-value type scale.
export const PROMPT_CONTROL_TYPOGRAPHY_CLASS = "prompt-control-type petite-caps"
