// Collapsed tool summaries and transcript events are peer metadata labels. Keep their type scale,
// tone, and line box shared so alternating `N tool calls` / `Thought for Ns` rows have one rhythm.
export const TRANSCRIPT_META_LABEL_CLASS = "petite-caps text-[12px] leading-[18px] text-muted/55"
